let names = ["James", "Alicia", "Fatiha", "Maria", "Bert"];
names.sort();
names.reverse();
console.log(names);

let bits = [0,1,2,4,8,16,32,64,128,256,512,1024];
bits.sort();
bits.reverse(); // Doesn't work the same with numbers
console.log(bits)
// Reversing
